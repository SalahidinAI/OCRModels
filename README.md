# 📸 OCR Сравнение с подсветкой различий

Система для сравнения и анализа качества распознавания текста (OCR) с использованием нескольких популярных OCR-моделей. Проект позволяет загружать изображения с текстом или таблицами и сравнивать результаты распознавания от разных OCR-движков с детальными метриками и отчётами.

## 🎯 Основные возможности

- **Распознавание текста**: Поддержка английского (EN) языка
- **Множественные OCR-модели**:
  - PaddleOCR (EN)
  - Tesseract (EN)
  - EasyOCR (EN)
- **Распознавание таблиц**: Использование PPStructure для извлечения структуры таблиц с гибридным подходом к распознаванию текста в ячейках
- **Сравнение с метриками**: 
  - Посимвольное сравнение результатов
  - Метрики схожести (Similarity)
  - Character Error Rate (CER)
  - Word Error Rate (WER)
  - Время обработки
- **Анализ результатов**: Автоматический анализ и рекомендации по выбору лучшего движка
- **Экспорт результатов**: Скачивание распознанного текста, таблиц (CSV/Excel) и отчётов (TXT/JSON)

## ⚙️ Технологический стек

- **Python**: 3.10
- **Интерфейс**: Streamlit 1.51.0
- **OCR-библиотеки**:
  - PaddleOCR 2.6.1.3
  - EasyOCR 1.7.1
  - pytesseract 0.3.13
- **Обработка изображений**: OpenCV 4.6.0.66, Pillow 12.0.0
- **Обработка данных**: Pandas 2.3.3, NumPy 1.26.4
- **Метрики**: RapidFuzz 3.14.3
- **Среда**: macOS (M1/M2/M3, ARM64)

## 📋 Требования

- macOS с процессором Apple Silicon (M1/M2/M3)
- Python 3.10 (обязательно)
- Homebrew (для установки Tesseract)
- Минимум 8 ГБ RAM (рекомендуется 16 ГБ для работы с большими изображениями)
- ~5 ГБ свободного места на диске (для моделей OCR)

## 🚀 Установка для macOS M1

### Шаг 1: Проверка Python

Убедитесь, что установлен Python 3.10:

```bash
python3.10 --version
# Должно показать: Python 3.10.x
```

Если Python 3.10 не установлен, установите через Homebrew:

```bash
brew install python@3.10
```

### Шаг 2: Клонирование репозитория

```bash
git clone <repository-url>
cd OCR.
```

**⚠️ Важно для GitHub**: В репозитории исключены большие файлы через `.gitignore`:
- Виртуальное окружение (`venv310/`)
- Кэш Python (`__pycache__/`)
- Модели OCR (загружаются автоматически при первом запуске)
- Тестовые изображения (опционально)

Модели OCR (PaddleOCR, EasyOCR) будут автоматически загружены при первом запуске приложения.

### Шаг 3: Создание виртуального окружения

```bash
python3.10 -m venv venv310
source venv310/bin/activate
```

**Проверка**: После активации в начале строки терминала должно появиться `(venv310)`.

### Шаг 4: Установка Tesseract OCR

```bash
# Установка через Homebrew
brew install tesseract
brew install tesseract-lang  # Для поддержки дополнительных языков (RU, CH)

# Проверка установки
tesseract --version

# Найти путь к Tesseract (важно для настройки)
which tesseract
# Обычно для M1: /opt/homebrew/bin/tesseract
```

**Важно**: Система автоматически определит путь к Tesseract, но если возникнут проблемы, путь можно проверить в `utils/config.py`.

### Шаг 5: Установка зависимостей Python

#### 5.1. Обновление pip

```bash
pip install --upgrade pip setuptools wheel
```

#### 5.2. Установка PaddlePaddle для macOS M1

**Критически важно**: PaddlePaddle требует специальной установки для Apple Silicon.

**Вариант A: Через pip (рекомендуется)**

```bash
# Установка PaddlePaddle для M1
pip install paddlepaddle==3.0.0b1 -f https://www.paddlepaddle.org.cn/whl/mkl/stable.html
```

**Вариант B: Через conda (альтернатива)**

Если pip не работает, используйте conda:

```bash
# Создать conda окружение
conda create -n ocr python=3.10
conda activate ocr

# Установить PaddlePaddle
conda install -c conda-forge paddlepaddle
```

#### 5.3. Установка остальных зависимостей

```bash
# Установка из requirements.txt
pip install -r requirements.txt
```

**Примечание**: 
- Установка может занять 10-30 минут
- PaddleOCR и EasyOCR автоматически загрузят предобученные модели при первом запуске (~2-3 ГБ)
- При первом запуске EasyOCR может запросить подтверждение загрузки моделей

### Шаг 6: Настройка переменных окружения

Для macOS M1 рекомендуется установить следующие переменные окружения:

```bash
# В терминале (для текущей сессии)
export PYTORCH_ENABLE_MPS_FALLBACK=1
export KMP_DUPLICATE_LIB_OK=TRUE
export MKL_SERVICE_FORCE_INTEL=1
export OMP_NUM_THREADS=4
```

**Для постоянной настройки**, добавьте в `~/.zshrc` (или `~/.bash_profile`):

```bash
echo 'export PYTORCH_ENABLE_MPS_FALLBACK=1' >> ~/.zshrc
echo 'export KMP_DUPLICATE_LIB_OK=TRUE' >> ~/.zshrc
echo 'export MKL_SERVICE_FORCE_INTEL=1' >> ~/.zshrc
echo 'export OMP_NUM_THREADS=4' >> ~/.zshrc
source ~/.zshrc
```

Эти переменные можно установить в терминале перед запуском Streamlit.

### Шаг 7: Проверка установки

Запустите тестовый скрипт:

```bash
# Убедитесь, что виртуальное окружение активировано
source venv310/bin/activate

# Запуск теста
python test_ocr.py test.jpeg
```

Если всё установлено правильно, вы увидите результаты тестирования всех OCR-движков.

## 🎮 Использование

### Запуск приложения

```bash
# Активировать виртуальное окружение
source venv310/bin/activate

# Запустить Streamlit
streamlit run main.py
```

Приложение откроется в браузере по адресу `http://localhost:8501`

### Работа с интерфейсом

1. **Загрузка изображения**: 
   - Нажмите "Browse files" или перетащите изображение в область загрузки
   - Поддерживаемые форматы: PNG, JPG, JPEG
   - Максимальный размер файла: 200 МБ

2. **Выбор опций**:
   - Отметьте "Распознать текст" для распознавания текста
   - Отметьте "Распознать таблицы" для извлечения таблиц

3. **Распознавание**:
   - Нажмите кнопку "🔍 Распознать и сравнить"
   - Система обработает изображение всеми доступными OCR-моделями
   - Результаты будут отображены с метриками сравнения

4. **Просмотр результатов**:
   - **Статистика совпадений**: Таблица попарных сравнений движков
   - **Время обработки**: Сравнение скорости работы движков
   - **Метрики ошибок**: CER и WER для каждого движка
   - **Анализ**: Автоматические рекомендации по выбору лучшего движка
   - **Результаты по движкам**: Текст, распознанный каждым движком

5. **Экспорт**:
   - Скачать объединённый текст
   - Скачать текст от каждого движка отдельно
   - Скачать таблицы в формате CSV или Excel
   - Скачать текстовый или JSON-отчёт

### Тестирование через командную строку

```bash
# Тест на стандартном изображении
python test_ocr.py

# Тест на конкретном изображении
python test_ocr.py path/to/image.jpg
```

## 📁 Структура проекта

```
OCR./
├── main.py                  # Основной файл Streamlit-приложения
├── test_ocr.py              # Тестовый скрипт для проверки всех движков
├── requirements.txt         # Список зависимостей Python
├── .gitignore              # Исключения для Git (большие файлы, модели)
├── utils/                   # Модули утилит
│   ├── __init__.py
│   ├── config.py            # Конфигурация и пути
│   ├── ocr_engines.py       # Управление OCR-движками
│   ├── table_processor.py   # Обработка таблиц через PPStructure
│   ├── text_utils.py        # Утилиты для работы с текстом
│   ├── metrics.py           # Метрики сравнения (CER, WER, Similarity)
│   ├── comparison.py        # Основной класс сравнения
│   └── report_generator.py  # Генерация отчётов
└── README.md                # Документация проекта
```

**Примечание**: 
- `venv310/` (виртуальное окружение) исключено из Git через `.gitignore`
- Модели OCR загружаются автоматически при первом запуске
- Тестовые изображения также исключены (можно добавить свои)

## 🔧 Технические детали

### Архитектура распознавания

#### Распознавание текста

Система использует 3 OCR-движка для английского языка:

1. **PaddleOCR**: Английский язык, высокая точность
2. **Tesseract**: Английский язык
3. **EasyOCR**: Английский язык

Все результаты объединяются, дубликаты удаляются через нормализацию текста.

#### Распознавание таблиц

⚠️ **Важно**: Для распознавания таблиц используется **PPStructure**, а не встроенная табличная опция PaddleOCR.

Процесс распознавания таблиц:

1. **Детекция структуры**: PPStructure определяет расположение таблиц и ячеек на изображении
2. **Извлечение ячеек**: Для каждой ячейки извлекается область изображения (ROI)
3. **Предобработка**: 
   - Увеличение изображения (2x) для лучшего распознавания
   - Конвертация в оттенки серого
   - Бинаризация (Otsu threshold)
4. **Гибридное распознавание текста в ячейках**:
   - Приоритет 1: Tesseract (обычно лучший для структурированного текста)
   - Приоритет 2: EasyOCR EN (если Tesseract не дал результата)
5. **Формирование таблицы**: Результаты собираются в структурированную HTML-таблицу, которая конвертируется в DataFrame

### Метрики сравнения

Система вычисляет следующие метрики:

- **Similarity (Схожесть)**: Процент совпадения между результатами (0-100%)
  - Использует комбинацию Token Sort Ratio, Partial Ratio, Token Set Ratio
- **CER (Character Error Rate)**: Процент ошибок на уровне символов
- **WER (Word Error Rate)**: Процент ошибок на уровне слов
- **Processing Time**: Время обработки каждого движка

### Нормализация текста

Для сравнения результатов используется функция нормализации:
- Удаление специальных символов (кроме букв, цифр, пробелов)
- Сохранение кириллицы (U+0400-U+04FF) и китайских иероглифов (U+4E00-U+9FFF)
- Приведение к нижнему регистру
- Удаление лишних пробелов

### Кэширование моделей

Модели OCR кэшируются с помощью `@st.cache_resource` для ускорения последующих запросов в Streamlit.

## 🐛 Решение проблем

### Проблема: Tesseract не найден

**Симптомы**: Ошибка "Tesseract не найден" в интерфейсе

**Решение**: 
1. Убедитесь, что Tesseract установлен: `brew list tesseract`
2. Проверьте путь: `which tesseract`
3. Система автоматически определит путь, но если не работает, проверьте `utils/config.py`
4. Для M1 путь обычно: `/opt/homebrew/bin/tesseract`

### Проблема: PaddleOCR ошибка инициализации

**Симптомы**: Ошибка при загрузке PaddleOCR

**Решение**:
1. Убедитесь, что установлен правильный PaddlePaddle для M1:
   ```bash
   pip uninstall paddlepaddle
   pip install paddlepaddle==3.0.0b1 -f https://www.paddlepaddle.org.cn/whl/mkl/stable.html
   ```
2. Проверьте переменные окружения (см. Шаг 6 установки)
3. Если проблема сохраняется, попробуйте установить через conda

### Проблема: EasyOCR не загружается

**Симптомы**: EasyOCR недоступен или долго загружается

**Решение**:
- EasyOCR автоматически загружает модели при первом запуске
- Убедитесь в наличии интернет-соединения
- Проверьте свободное место на диске (модели занимают ~500 МБ)
- Первый запуск может занять 5-10 минут

### Проблема: Медленная обработка

**Симптомы**: Обработка изображения занимает очень много времени

**Рекомендации**:
- Используйте изображения с разрешением не более 3000x3000 пикселей
- Для больших изображений предварительно уменьшите размер
- Убедитесь, что используется достаточное количество RAM (минимум 8 ГБ)
- Закройте другие приложения, использующие много памяти

### Проблема: Ошибки с таблицами

**Симптомы**: Таблицы не распознаются или распознаются неправильно

**Решение**:
- Убедитесь, что PPStructure инициализирован корректно (проверьте статус в интерфейсе)
- Проверьте, что изображение содержит чётко видимые границы таблиц
- Для сложных таблиц может потребоваться предобработка изображения (увеличение контраста, бинаризация)

### Проблема: Ошибка импорта модулей

**Симптомы**: `ModuleNotFoundError` при запуске

**Решение**:
1. Убедитесь, что виртуальное окружение активировано
2. Проверьте, что все зависимости установлены: `pip install -r requirements.txt`
3. Убедитесь, что вы находитесь в корневой директории проекта

### Проблема: Ошибка при загрузке в GitHub (большие файлы)

**Симптомы**: GitHub отклоняет push из-за больших файлов (>100MB)

**Решение**:
1. Убедитесь, что `.gitignore` правильно настроен (включены `venv310/`, `__pycache__/`, модели OCR)
2. Если большие файлы уже были закоммичены, удалите их из истории:
   ```bash
   # Удалить venv310 из истории (если был закоммичен)
   git rm -r --cached venv310/
   git commit -m "Remove venv310 from repository"
   
   # Проверить размер репозитория
   git count-objects -vH
   ```
3. Если проблема сохраняется, создайте новый репозиторий без истории:
   ```bash
   # Удалить .git и создать новый репозиторий
   rm -rf .git
   git init
   git add .
   git commit -m "Initial commit"
   ```
4. Модели OCR не должны быть в репозитории - они загружаются автоматически при первом запуске

## 📊 Примеры использования

### Базовое распознавание текста

```bash
streamlit run main.py
```

Затем загрузите изображение через интерфейс.

### Тестирование всех движков

```bash
python test_ocr.py test.jpeg
```

### Программное использование

```python
from utils import OCRComparison

# Инициализация
ocr = OCRComparison()

# Обработка изображения
results = ocr.process_image("image.jpg")

# Сравнение результатов
comparison = ocr.compare_text_results(results)

# Экспорт
exports = ocr.export_results(results)
```

## 🔮 Планы развития

- [ ] Добавление поддержки других OCR-библиотек (TrOCR, MMOCR)
- [ ] Улучшение визуализации сравнения результатов (графики, heatmaps)
- [ ] Добавление интерактивного сравнения текста с подсветкой различий
- [ ] Поддержка PDF-файлов
- [ ] Batch-обработка нескольких изображений
- [ ] API для интеграции с другими системами
- [ ] Кэширование результатов для повторного использования
- [ ] Настройка параметров OCR для каждого движка

## 📝 Примечания

- Проект оптимизирован для macOS с процессором Apple Silicon (M1/M2/M3)
- Для работы на других платформах может потребоваться корректировка путей и зависимостей
- Первый запуск может занять больше времени из-за загрузки моделей OCR
- Рекомендуется использовать виртуальное окружение для изоляции зависимостей
- Система автоматически определяет путь к Tesseract на macOS

## 📄 Лицензия

[Укажите лицензию проекта]

## 👥 Авторы

[Укажите авторов проекта]

## 🙏 Благодарности

- PaddleOCR team за отличную библиотеку и PPStructure
- EasyOCR за простоту использования
- Tesseract OCR за надёжность и многоязычность
- Streamlit за удобный фреймворк для веб-интерфейсов

---

**Версия**: 1.0.0  
**Последнее обновление**: 2025  
**Поддержка**: macOS M1/M2/M3, Python 3.10
