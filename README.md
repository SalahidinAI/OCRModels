# 📸 OCR Сравнение с подсветкой различий

Система для сравнения и анализа качества распознавания текста (OCR) с использованием нескольких популярных OCR-моделей. Проект позволяет загружать изображения с текстом или таблицами и сравнивать результаты распознавания от разных OCR-движков с детальными метриками и визуализацией различий.

## 🎯 Основные возможности

- **Множественные OCR-модели**: PaddleOCR, Tesseract, EasyOCR
- **Поддержка языков**: Английский (EN), Русский (RU), Китайский (CH)
- **Распознавание таблиц**: Использование PPStructure для извлечения структуры таблиц
- **Сравнение результатов**: 
  - Пословное сравнение с цветовой подсветкой различий
  - Метрики совпадений (полное, частичное, расхождения)
  - Визуализация статистики совпадений
- **Экспорт результатов**: Скачивание распознанного текста, таблиц (CSV/Excel)

## ⚙️ Технологический стек

### Основные компоненты

- **Python**: 3.10 (обязательно)
- **Streamlit**: 1.51.0 - веб-интерфейс
- **Pandas**: 2.3.3 - обработка данных и таблиц
- **NumPy**: 1.26.4 - численные операции

### OCR-библиотеки

- **PaddleOCR**: 2.6.1.3 - основной OCR-движок с поддержкой множества языков
- **PaddlePaddle**: 3.0.0b1 - фреймворк для PaddleOCR (требует специальной установки для Apple Silicon)
- **EasyOCR**: 1.7.1 - простой в использовании OCR-движок
- **pytesseract**: 0.3.13 - Python-обёртка для Tesseract OCR

### Обработка изображений

- **OpenCV**: 4.6.0.66 - обработка и предобработка изображений
- **Pillow**: 12.0.0 - работа с изображениями

### Метрики и сравнение

- **RapidFuzz**: 3.14.3 - быстрые алгоритмы сравнения строк (fuzzy matching)

### Экспорт данных

- **openpyxl**: 3.1.5 - экспорт таблиц в Excel

### Системные требования

- **ОС**: macOS (M1/M2/M3), Linux, Windows
- **Python**: 3.10 (строго)
- **RAM**: минимум 8 ГБ (рекомендуется 16 ГБ)
- **Диск**: ~5 ГБ свободного места (для моделей OCR)
- **Tesseract**: требуется установка через системный пакетный менеджер

## 📋 Установка

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd OCR.
```

### 2. Создание виртуального окружения

```bash
python3.10 -m venv venv310
source venv310/bin/activate  # Linux/macOS
# или
venv310\Scripts\activate  # Windows
```

### 3. Установка Tesseract

**macOS:**
```bash
brew install tesseract
brew install tesseract-lang  # Для поддержки дополнительных языков
```

**Linux (Ubuntu/Debian):**
```bash
sudo apt-get install tesseract-ocr
sudo apt-get install tesseract-ocr-rus tesseract-ocr-chi-sim  # Для RU и CH
```

**Windows:**
Скачайте установщик с [GitHub Tesseract](https://github.com/UB-Mannheim/tesseract/wiki)

### 4. Установка PaddlePaddle (для Apple Silicon)

**macOS M1/M2/M3:**
```bash
pip install paddlepaddle==3.0.0b1 -f https://www.paddlepaddle.org.cn/whl/mkl/stable.html
```

**Linux/Windows:**
```bash
pip install paddlepaddle
```

### 5. Установка зависимостей

```bash
pip install --upgrade pip setuptools wheel
pip install -r requirements.txt
```

**Примечание**: 
- Установка может занять 10-30 минут
- PaddleOCR и EasyOCR автоматически загрузят модели при первом запуске (~2-3 ГБ)
- При первом запуске EasyOCR может запросить подтверждение загрузки моделей

### 6. Настройка переменных окружения (опционально, для macOS M1)

```bash
export PYTORCH_ENABLE_MPS_FALLBACK=1
export KMP_DUPLICATE_LIB_OK=TRUE
export MKL_SERVICE_FORCE_INTEL=1
export OMP_NUM_THREADS=4
```

## 🚀 Использование

### Запуск приложения

```bash
source venv310/bin/activate  # Активировать виртуальное окружение
streamlit run main.py
```

Приложение откроется в браузере по адресу `http://localhost:8501`

### Работа с интерфейсом

1. **Загрузка изображения**: Перетащите изображение или нажмите "Browse files"
   - Поддерживаемые форматы: PNG, JPG, JPEG
   - Максимальный размер: 200 МБ

2. **Выбор опций**:
   - "Распознать текст" - распознавание текста всеми моделями
   - "Распознать таблицы" - извлечение таблиц через PPStructure

3. **Просмотр результатов**:
   - Метрики совпадений (полное, частичное, расхождения)
   - График распределения совпадений
   - Результаты по движкам с цветовой подсветкой:
     - 🟢 Зелёный - все модели совпадают
     - 🟡 Жёлтый - две модели совпадают
     - 🔴 Красный - только одна модель или расхождения

4. **Экспорт**:
   - Скачать текст от каждого движка
   - Скачать таблицы в CSV или Excel

## 📁 Структура проекта

```
OCR./
├── main.py                  # Основной файл Streamlit-приложения
├── requirements.txt         # Список зависимостей Python
├── .gitignore              # Исключения для Git
├── utils/                   # Модули утилит
│   ├── __init__.py
│   ├── config.py            # Конфигурация и пути (Tesseract, языки)
│   ├── ocr_engines.py       # Управление OCR-движками (инициализация, загрузка)
│   ├── comparison.py        # Основной класс сравнения результатов
│   ├── table_processor.py   # Обработка таблиц через PPStructure
│   ├── text_utils.py        # Утилиты для работы с текстом (нормализация, сравнение)
│   ├── metrics.py           # Метрики сравнения (CER, WER, Similarity)
│   └── report_generator.py # Генерация отчётов (TXT, JSON)
└── README.md                # Документация проекта
```

## 🔧 Компоненты системы

### `main.py`
Главный файл Streamlit-приложения. Содержит:
- Инициализацию OCR-системы
- Интерфейс загрузки изображений
- Отображение результатов с метриками и визуализацией
- Экспорт результатов

### `utils/config.py`
Конфигурация системы:
- Определение путей к Tesseract (автоматическое для разных ОС)
- Поддерживаемые языки (EN, RU, CH)
- Настройки форматов изображений
- Переменные окружения для Apple Silicon

### `utils/ocr_engines.py`
Управление OCR-движками:
- **OCREngineManager**: Класс для управления всеми OCR-движками
- Инициализация PaddleOCR, EasyOCR, Tesseract
- Загрузка моделей с отслеживанием времени
- Распознавание текста с поддержкой множественных языков
- Кэширование загруженных моделей

### `utils/comparison.py`
Основной класс сравнения:
- **OCRComparison**: Главный класс системы
- Обработка изображений (текст и таблицы)
- Сравнение результатов от разных движков
- Анализ различий

### `utils/text_utils.py`
Утилиты для работы с текстом:
- Нормализация текста (удаление спецсимволов, сохранение кириллицы и китайских иероглифов)
- Пословное сравнение текстов с определением совпадений
- Выравнивание текстов для сравнения

### `utils/metrics.py`
Метрики сравнения:
- **calculate_similarity**: Расчёт схожести текстов (0-100%)
- **calculate_character_error_rate**: CER (Character Error Rate)
- **calculate_word_error_rate**: WER (Word Error Rate)
- **compare_results**: Сравнение результатов от разных движков

### `utils/table_processor.py`
Обработка таблиц:
- **TableProcessor**: Класс для распознавания таблиц
- Использование PPStructure для детекции структуры
- Гибридное распознавание текста в ячейках (Tesseract + EasyOCR)
- Предобработка изображений (увеличение, бинаризация)
- Экспорт в HTML, CSV, Excel

### `utils/report_generator.py`
Генерация отчётов:
- **ReportGenerator**: Класс для создания отчётов
- Текстовые отчёты (TXT)
- JSON-отчёты с полной информацией
- Метрики и статистика

## 🔍 Детали зависимостей

### PaddleOCR и PaddlePaddle
- **PaddleOCR**: Высокопроизводительная OCR-библиотека с поддержкой множества языков
- **PaddlePaddle**: Глубокий фреймворк для машинного обучения
- **Особенности**: Требует специальной установки для Apple Silicon (M1/M2/M3)
- **Модели**: Автоматически загружаются при первом использовании (~1-2 ГБ)

### EasyOCR
- **Простота**: Легко использовать, автоматическая загрузка моделей
- **Языки**: Поддержка 80+ языков
- **Модели**: Загружаются автоматически при первом запуске (~500 МБ)

### Tesseract
- **Надёжность**: Проверенная временем OCR-библиотека
- **Установка**: Требует системной установки (не через pip)
- **Языки**: Поддержка 100+ языков через языковые пакеты

### RapidFuzz
- **Производительность**: Быстрые алгоритмы нечёткого сравнения строк
- **Использование**: Сравнение результатов OCR для определения схожести

## 🐛 Решение проблем

### Tesseract не найден
```bash
# Проверка установки
tesseract --version
which tesseract

# Система автоматически определит путь, но можно проверить в utils/config.py
```

### PaddleOCR ошибка инициализации (macOS M1)
```bash
# Переустановка PaddlePaddle для M1
pip uninstall paddlepaddle
pip install paddlepaddle==3.0.0b1 -f https://www.paddlepaddle.org.cn/whl/mkl/stable.html
```

### Медленная обработка
- Используйте изображения с разрешением не более 3000x3000 пикселей
- Убедитесь в наличии достаточного количества RAM (минимум 8 ГБ)
- Закройте другие приложения, использующие много памяти

## 📝 Лицензия

[Укажите лицензию проекта]

## 👥 Авторы

[Укажите авторов проекта]

---

**Версия**: 2.0.0  
**Последнее обновление**: 2025  
**Поддержка**: macOS (M1/M2/M3), Linux, Windows | Python 3.10
